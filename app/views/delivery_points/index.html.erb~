<h1>Listing delivery_points</h1>

<table>
  <tr>
    <th>Address</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>Delivery area</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @delivery_points.each do |delivery_point| %>
  <tr>
    <td><%= delivery_point.address %></td>
    <td><%= delivery_point.latitude %></td>
    <td><%= delivery_point.longitude %></td>
    <td><%= delivery_point.customer %></td>
    <td><%= delivery_point.delivery_area.name %></td>
    <td><%= link_to 'Show', delivery_point %></td>
    <td><%= link_to 'Edit', edit_delivery_point_path(delivery_point) %></td>
    <td><%= link_to 'Destroy', delivery_point, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Delivery point', new_delivery_point_path %>
