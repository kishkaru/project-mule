<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
<h2>LuckyBolt Order Confirmation, <%= @order.created_at %></h2>

<h3>Thank you for your order! Below is your order summary. </h3>
<h4>Order Summary </h4>

<table>
    <thead>
        <tr>
          <th> Name </th>
          <th> Vendor </th>
          <th> Quantity </th>
          <th> Item Price </th>
          <th> Total Price </th>
        </tr>
    </thead>
    <tbody>
        <% @items.each do |order_item| %>
            <% item = order_item.item %>
        <tr>
            <td> <%= item.name %> </td>
            <td> <%= item.vendor.name %> </td>
            <td> <%= order_item.quantity %> </td>
            <td> <%= number_to_currency item.price %> </td>
            <td> <%= number_to_currency item.price*order_item.quantity %> </td>
        </tr>
        <% end %>
    </tbody>
</table>
<p>Subtotal: <%= number_to_currency @order.subtotal %> <br>
Tax: <%= number_to_currency @order.tax_total %> <br>
Total: <%= number_to_currency @order.total %>
</p>

<p>
  <h4>Payment Information </h4>
  Transaction Date: <%= @transaction.created_at %> <br>
  Transaction Amount: <%= @transaction.amount %> <br>
  Card Type: <%= @transaction.credit_card_details.card_type %> <br>
  Card Number: <%= @transaction.credit_card_details.masked_number %> <br>
</p>

<p>
  <h4>Pickup Information </h4>
  You will recieve an email or text message when your order is ready for pickup
  at the following delivery point: <br>
  <%= @order.delivery_point.address.to_s %>
</p>
<p>Thanks and have a wonderful lunch!</p>

</body>
</html>